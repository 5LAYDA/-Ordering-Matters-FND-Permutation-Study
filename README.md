# -Ordering-Matters-FND-Permutation-Study
# Ordering Matters: FND Permutation Study — Colab Notebook  **Companion notebook for the Q1 Elsevier paper.**  This notebook performs the **real experimental protocol** of the paper:  - **Layer L1** (always runs, CPU-only, ~1 minute): 4 classical baselines (LR, SVM, RF, SGD on TF-IDF) × 3 datasets × 13 random seeds = **156 real training runs**
